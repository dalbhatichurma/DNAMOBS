package com.dnamobs.models;

/**
 * Enum representing different damage types for DNA tracking
 */
public enum DamageType {
    MELEE,
    RANGED,
    FIRE,
    EXPLOSION,
    MAGIC,
    ENVIRONMENTAL,
    UNKNOWN
}
